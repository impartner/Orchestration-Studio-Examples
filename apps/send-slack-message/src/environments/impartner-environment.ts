// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `impartner-environment.ts` with `impartner-environment.prod.ts`.
// The list of file replacements can be found in `project.json`.

export const impartnerEnvironment = {
  production: false,
  impartnerEndpointUrl: '/api'
};
